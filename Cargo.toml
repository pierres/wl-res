[package]
name = "wl-res"
version = "0.1.0"
edition = "2024"
description = "Tiny Wayland CLI for printing the primary display's resolution, with aspect-ratio fitting"

[dependencies]
wayland-client = "0.31"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
